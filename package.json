{
  "name": "devil-enhancer-app",
  "version": "1.0.0",
  "private": true,
  "description": "Devil Enhancer - premium video enhancement app UI built with Capacitor.",
  "scripts": {
    "start": "npx serve www",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:web": "mkdir -p dist && cp -r www/* dist/"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "serve": "^14.2.4"
  }
}